= Конфигурация {of-fs}

[#minimum]
== Необходимые настройки конфигурационного файла

Ниже перечисленны минимально необходимые настройки конфигурационного файла {of-fs} по адресу `/usr/lib/docsvision/fileservice/appsettings.json`. Файл необходимо заполнять в любом случае при свежей установке системы, при обновлении системы с предыдущих версий можно принять решение оставить существующий конфигурационный файл.

[source,json]
----
{
  "DocsVision": {
    "ServiceId": "00000000-0000-0000-0000-000000000000", <.>
    "SettingsService": {
      "ConnectionString": "http://localhost:5200/api", <.>
      "ApiKey": "anything" <.>
    }
  },
}
----
<.> `ServiceId` -- уникальный идентификатор Сервера приложений, с базами которого будет работать серверная консоль.
<.> `ConnectionString` -- адрес {of-sett-serv}.
<.> `ApiKey` -- API-ключ {of-sett-serv}.

[#console]
== Указание БД из {of-mc}

// tag::console[]
. В {of-mc} перейдите на страницу menu:Серверы[Имя сервера > {fs} > xref:dev@mgmtconsole:servers:fileservice-database.adoc[Группа настроек "Базы данных"]], чтобы подключить БД к Файловому сервису.
+
.Группа настроек "Базы данных" для файлового сервиса
image::dev@mgmtconsole:servers:fileservice-database.png[Группа настроек "Базы данных" для файлового сервиса]
+
. Нажмите кнопку *Подключить* под таблицей, чтобы открыть окно подключения БД из доступных к файловому сервису.
+
.Окно подключения БД для использования в файловом сервисе
image::dev@mgmtconsole:servers:fileservice-database-connect.png[Окно подключения БД для использования в файловом сервисе]
// end::console[]

// [#config]
// == Указание БД в конфигурационном файле
//
// . Укажите существующую БД, заполнив строку подключения к БД в конфигурационном файле:
// +
// [source,json]
// ----
// {
//   "DocsVision": {
//     "Server": {
//       "Databases": {
//         "alias": "Database-connection-string" <.>
//       }
//     }
//   },
// }
// ----
// <.> `alias` -- строка подключения к БД.
//
// include::new-admin-console@platform:admin:config-platform.adoc[tags=c-string]
